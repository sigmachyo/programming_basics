#include <iostream>
using namespace std;
int main()
{
	/* 1 задание
	double n, k, s;
	double t;
	cin >> n >> k >> s;
	t = k / 100 * s / 12 * n;
	cout << t;
	*/

	// 3 задание, вроде мое
	double v1, v2, t1, t2;
	cin >> v1 >> v2 >> t1 >> t2;
	cout << (v1 + v2) << ' ' << ((v1 * t1 + v2 * t2) / (v1 + v2));



}


